(()=>{
"use strict";

const HISTORY_KEY="evia-arp-discussion-v1";
const MOCK_SIZE=5;
const PROMPTS=["What","How","Why","Check","Example"];
const STOP_WORDS=new Set(["about","after","again","against","also","and","are","because","been","before","being","between","could","from","have","into","must","only","other","should","that","the","their","them","then","there","these","they","this","through","using","what","when","where","which","while","with","would","your"]);
let session=null;
let voice=null;

function esc(value){return String(value??"").replace(/[&<>"']/g,char=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[char]))}
function sentence(value){const text=String(value??"").trim();if(!text)return"";return /[.!?]$/.test(text)?text:text+"."}
function body(){return document.querySelector(".evia-arp-layer .evia-tools-body")}
function shuffled(items){const output=[...items];for(let index=output.length-1;index>0;index--){const pick=Math.floor(Math.random()*(index+1));[output[index],output[pick]]=[output[pick],output[index]]}return output}
function followUpFor(question){const followUps=["Why is that approach important on a live job?","How would you check that the work or control was correct?","What could happen if that point was missed?","Which workplace example or portfolio evidence would support your answer?"];const hash=String(question.id||question.prompt||"").split("").reduce((total,char)=>total+char.charCodeAt(0),0);return followUps[hash%followUps.length]}
function responsesFor(question){const core=sentence(question.options?.[Number(question.correctIndex)]),reason=sentence(question.explanation);return [{strength:4,text:core+" "+reason+" I would connect this to a workplace example, describe the check and result, and explain anything I recorded or reported."}]}
function discussionItems(bank){if(!bank||!Array.isArray(bank.questions))return[];return bank.questions.map(question=>({id:String(question.id).replace("-Q","-D"),sourceQuestionId:question.id,topic:question.topic,mapsTo:[...(question.mapsTo||[])],prompt:question.prompt,coreAnswer:question.options?.[Number(question.correctIndex)]||"",reason:question.explanation||"",responses:responsesFor(question),followUp:followUpFor(question)}))}
function history(){try{const parsed=JSON.parse(localStorage.getItem(HISTORY_KEY)||"{}");return parsed&&typeof parsed==="object"?parsed:{}}catch{return{}}}
function historyFor(id){const item=history()[id];return item&&typeof item==="object"?item:{attempts:0,bestScore:0,lastScore:0}}
function saveAttempt(id,score,total){const all=history(),before=historyFor(id),percent=Math.round(score/Math.max(1,total)*100);all[id]={...before,attempts:Number(before.attempts||0)+1,bestScore:Math.max(Number(before.bestScore||0),score),bestPercent:Math.max(Number(before.bestPercent||0),percent),lastScore:score,lastPercent:percent,lastMode:"mock",updatedAt:Date.now()};try{localStorage.setItem(HISTORY_KEY,JSON.stringify(all))}catch{}return all[id]}
function readiness(score,total){const percent=Math.round(score/Math.max(1,total)*100);if(percent>=85)return{label:"Strong",copy:"You consistently demonstrated detailed answers with checks and workplace evidence."};if(percent>=70)return{label:"Secure",copy:"Your answers are secure. Add a workplace result or portfolio example more consistently."};if(percent>=50)return{label:"Developing",copy:"Your technical points are useful, but more explanation and checking detail is needed."};return{label:"Building",copy:"Keep the correct point, then build each answer with how, why, a check and an example."}}
function patchDiscussionRow(){const button=document.querySelector('.evia-arp-layer [data-arp-option="discussion"]');if(!button)return;const small=button.querySelector("small");if(small)small.textContent="24 course-specific scenarios · mock discussion";button.setAttribute("aria-label","Open Mock Discussion")}
function returnToArp(){releaseVoice();session=null;window.EviaArp?.open?.()}
async function currentBank(){if(!window.EviaArp?.currentBank)throw Error("The ARP course bank is not ready.");return window.EviaArp.currentBank()}

async function renderDiscussionHome(){
  releaseVoice();session=null;const target=body();if(!target)return;
  target.innerHTML='<div class="evia-arp-loading">Loading this course’s discussion scenarios…</div>';
  try{
    const bank=await currentBank(),items=discussionItems(bank),record=historyFor(bank.enrolmentId);
    target.innerHTML='<p class="evia-tools-kicker">ARP · Discussion</p><h2>Mock Discussion</h2><p class="evia-tools-copy">Answer five course-specific questions aloud, then compare and self-check your response.</p><div class="evia-arp-summary"><b>'+esc(bank.courseTitle)+'</b><span>'+items.length+' mapped scenarios · 5 questions per mock'+(record.attempts?' · best '+Number(record.bestScore||0)+'/20':'')+'</span></div><div class="evia-arp-modes"><button type="button" class="evia-arp-mode" data-discussion-mode="mock"><b>Mock Discussion</b><small>Answer five questions aloud without choices, then compare and self-check.</small><i>›</i></button></div><div class="evia-arp-strength-key"><span><b>1 · Correct but brief</b>Main point only</span><span><b>2 · Clear</b>Point and reason</span><span><b>3 · Well reasoned</b>Reason and check</span><span><b>4 · Thorough</b>Check and evidence</span></div><p class="evia-arp-disclaimer">This is private practice and does not predict an official assessment grade.</p><button type="button" class="evia-arp-secondary" data-discussion-back>Back to ARP</button>';
    target.querySelector("[data-discussion-mode=\"mock\"]")?.addEventListener("click",startDiscussion);
    target.querySelector("[data-discussion-back]")?.addEventListener("click",returnToArp)
  }catch(error){target.innerHTML='<p class="evia-tools-kicker">ARP · Discussion</p><h2>Discussion bank unavailable</h2><p class="evia-tools-copy">'+esc(error?.message||error)+'</p><button type="button" class="evia-arp-secondary" data-discussion-back>Back to ARP</button>';target.querySelector("[data-discussion-back]")?.addEventListener("click",returnToArp)}
}

async function startDiscussion(){
  releaseVoice();const target=body();if(!target)return;target.innerHTML='<div class="evia-arp-loading">Preparing five course-specific scenarios…</div>';
  try{const bank=await currentBank(),questions=shuffled(discussionItems(bank)).slice(0,MOCK_SIZE);session={kind:"discussion",mode:"mock",bank,questions,index:0,results:[],saved:false};renderMockQuestion()}catch(error){target.innerHTML='<h2>Discussion bank unavailable</h2><p class="evia-tools-copy">'+esc(error?.message||error)+'</p><button type="button" class="evia-arp-secondary" data-discussion-home>Back</button>';target.querySelector("[data-discussion-home]")?.addEventListener("click",renderDiscussionHome)}
}
function resultRows(results){return '<div class="evia-arp-result-list">'+results.map((result,index)=>'<div class="evia-arp-result-row"><b>'+(index+1)+'</b><span>'+esc(result.topic||"Trade discussion")+'</span><strong>'+result.strength+'/4</strong></div>').join("")+'</div>'}
function renderDiscussionResult(){
  const target=body();if(!target||!session)return;const score=session.results.reduce((total,result)=>total+Number(result.strength||0),0),total=MOCK_SIZE*4;
  if(!session.saved){session.record=saveAttempt(session.bank.enrolmentId,score,total);session.saved=true}
  const level=readiness(score,total);
  target.innerHTML='<p class="evia-tools-kicker">Mock complete</p><h2>'+esc(level.label)+' discussion response</h2><div class="evia-arp-score"><div><strong>'+score+'/'+total+'</strong><span>discussion strength</span></div></div><p class="evia-tools-copy">'+esc(level.copy)+'</p>'+resultRows(session.results)+'<div class="evia-arp-actions"><button type="button" class="evia-arp-secondary" data-discussion-retry>Try another mock</button><button type="button" class="evia-arp-secondary" data-discussion-home>Discussion home</button></div><p class="evia-arp-disclaimer">This readiness score is for practice only and is not an official EPA or qualification grade.</p>';
  target.querySelector("[data-discussion-retry]")?.addEventListener("click",startDiscussion);target.querySelector("[data-discussion-home]")?.addEventListener("click",renderDiscussionHome)
}
function speakQuestion(text){if(!window.speechSynthesis||!window.SpeechSynthesisUtterance)return;try{window.speechSynthesis.cancel();const utterance=new window.SpeechSynthesisUtterance(String(text));utterance.lang="en-GB";utterance.rate=.94;window.speechSynthesis.speak(utterance)}catch{}}
function promptMarkup(){return '<div class="evia-arp-prompt-grid" aria-label="Answer structure">'+PROMPTS.map(prompt=>'<span>'+prompt+'</span>').join("")+'</div>'}
function recorderMarkup(){return '<div class="evia-arp-voice-card"><b>Speak for about 60–90 seconds</b><p>Use What, How, Why, Check and Example. Evia does not upload the audio or add it to your portfolio. Any transcript shown is supplied by your browser.</p><div class="evia-arp-voice-controls"><button type="button" data-voice-start>Start voice answer</button><button type="button" class="secondary" data-voice-stop disabled>Stop and review</button><button type="button" class="secondary" data-voice-play>Play question</button><button type="button" class="secondary" data-voice-skip>Answered aloud without recording</button></div><div class="evia-arp-voice-status" data-voice-status aria-live="polite">Your recording stays in this practice screen and is discarded when you leave it.</div></div>'}
function stopTracks(stream){try{stream?.getTracks?.().forEach(track=>track.stop())}catch{}}
function stopRecognition(state){if(!state?.recognition)return;try{state.recognition.stop()}catch{}}
function releaseVoice(){const state=voice;if(!state)return;state.cancelled=true;if(state.timer)clearInterval(state.timer);try{if(state.recorder&&state.recorder.state!=="inactive")state.recorder.stop()}catch{}stopTracks(state.stream);try{state.recognition?.abort?.()}catch{}if(state.url)try{URL.revokeObjectURL(state.url)}catch{}voice=null}
function startRecognition(state){const Recognition=window.SpeechRecognition||window.webkitSpeechRecognition;if(!Recognition)return;try{const recognition=new Recognition();recognition.lang="en-GB";recognition.continuous=true;recognition.interimResults=false;recognition.onresult=event=>{let addition="";for(let index=event.resultIndex;index<event.results.length;index++)if(event.results[index].isFinal)addition+=" "+event.results[index][0].transcript;state.transcript=(state.transcript+addition).trim()};recognition.onerror=()=>{};recognition.start();state.recognition=recognition}catch{}}
async function startVoiceRecording(onReady){
  const target=body(),status=target?.querySelector("[data-voice-status]"),startButton=target?.querySelector("[data-voice-start]"),stopButton=target?.querySelector("[data-voice-stop]"),nav=window.navigator,Recorder=window.MediaRecorder;
  if(!nav?.mediaDevices?.getUserMedia||!Recorder){if(status)status.textContent="Voice recording is unavailable here. Say the answer aloud, then use the comparison option.";return}
  releaseVoice();
  try{const stream=await nav.mediaDevices.getUserMedia({audio:true});const types=["audio/webm;codecs=opus","audio/mp4","audio/webm"],mimeType=typeof Recorder.isTypeSupported==="function"?(types.find(type=>Recorder.isTypeSupported(type))||""):"",recorder=mimeType?new Recorder(stream,{mimeType}):new Recorder(stream),state={recorder,stream,chunks:[],startedAt:Date.now(),transcript:"",cancelled:false,url:null,recognition:null,timer:null};voice=state;recorder.addEventListener("dataavailable",event=>{if(event.data?.size)state.chunks.push(event.data)});recorder.addEventListener("stop",()=>{if(state.timer)clearInterval(state.timer);stopTracks(state.stream);stopRecognition(state);if(state.cancelled)return;const duration=Math.max(1,Math.round((Date.now()-state.startedAt)/1000)),blob=new Blob(state.chunks,{type:recorder.mimeType||"audio/webm"});state.url=URL.createObjectURL(blob);if(status){status.classList.remove("is-recording");status.textContent="Recording ready to review."}setTimeout(()=>{if(!state.cancelled)onReady({url:state.url,duration,transcript:state.transcript.trim()})},220)});recorder.start(250);startRecognition(state);if(startButton)startButton.disabled=true;if(stopButton)stopButton.disabled=false;if(status){status.classList.add("is-recording");status.textContent="Recording · 0:00"}state.timer=setInterval(()=>{const seconds=Math.max(0,Math.floor((Date.now()-state.startedAt)/1000));if(status)status.textContent="Recording · "+Math.floor(seconds/60)+":"+String(seconds%60).padStart(2,"0")},500)}catch{if(status)status.textContent="Microphone access was not available. Say the answer aloud, then use the comparison option."}
}
function stopVoiceRecording(){const state=voice;if(!state?.recorder||state.recorder.state==="inactive")return;const target=body(),stopButton=target?.querySelector("[data-voice-stop]"),status=target?.querySelector("[data-voice-status]");if(stopButton)stopButton.disabled=true;if(status)status.textContent="Preparing your recording…";try{state.recorder.stop()}catch{}}
function bindRecorder(question,onReady){const target=body();target?.querySelector("[data-voice-start]")?.addEventListener("click",()=>startVoiceRecording(onReady));target?.querySelector("[data-voice-stop]")?.addEventListener("click",stopVoiceRecording);target?.querySelector("[data-voice-play]")?.addEventListener("click",()=>speakQuestion(question.prompt));target?.querySelector("[data-voice-skip]")?.addEventListener("click",()=>{releaseVoice();onReady(null)})}
function words(value){return[...new Set(String(value||"").toLowerCase().replace(/[^a-z0-9\s-]/g," ").split(/\s+/).filter(word=>word.length>3&&!STOP_WORDS.has(word)))]}
function hasModelWords(transcript,model){const spoken=new Set(words(transcript)),wanted=words(model);if(!wanted.length)return false;const matches=wanted.filter(word=>spoken.has(word)).length;return matches>=Math.max(1,Math.ceil(wanted.length*.24))}
function coverageFlags(question,transcript){const text=String(transcript||"").toLowerCase();return{what:hasModelWords(text,question.coreAnswer),how:/\b(use|used|using|follow|followed|apply|applied|fit|fitted|install|installed|set|prepare|prepared|remove|removed|protect|protected|stop|report|reported)\b/.test(text),why:hasModelWords(text,question.reason)||/\b(because|therefore|means|prevent|prevents|reduce|reduces|avoid|ensures?|important)\b/.test(text),check:/\b(check|checked|confirm|confirmed|inspect|inspected|measure|measured|test|tested|verify|verified|level|plumb|square|datum|record|reported)\b/.test(text),example:/\b(example|evidence|portfolio|site|workplace|job|when i|i have|result|recorded|reported)\b/.test(text)}}
function audioMarkup(recording){if(!recording?.url)return"";return '<audio class="evia-arp-audio" controls preload="metadata" src="'+esc(recording.url)+'"></audio><p class="evia-arp-disclaimer">Recorded answer: '+recording.duration+' seconds'+(recording.duration<45?" · Try adding more explanation and a workplace example.":recording.duration>110?" · Aim to keep the next answer more focused.":" · Good rehearsal length.")+'</p>'}
function transcriptMarkup(recording){if(!recording?.transcript)return"";return '<div class="evia-arp-transcript"><b>Device transcript</b><br>'+esc(recording.transcript)+'</div>'}
function renderMockQuestion(){
  releaseVoice();const target=body();if(!target||!session)return;if(session.index>=MOCK_SIZE){renderDiscussionResult();return}const question=session.questions[session.index],number=session.index+1,progress=Math.round((session.index/MOCK_SIZE)*100);
  target.innerHTML='<div class="evia-arp-meta"><span>Mock question '+number+' of '+MOCK_SIZE+'</span><span>'+esc(session.bank.courseTitle)+'</span></div><div class="evia-arp-track" aria-hidden="true"><span style="width:'+progress+'%"></span></div><p class="evia-arp-topic">'+esc(question.topic)+'</p><h2 class="evia-arp-question">'+esc(question.prompt)+'</h2><p class="evia-tools-copy">No answer choices are shown in Mock Discussion. Speak naturally and support the answer with your own work.</p>'+promptMarkup()+recorderMarkup()+'<button type="button" class="evia-arp-secondary" data-mock-exit>Exit mock</button>';
  bindRecorder(question,recording=>renderMockReview(question,recording));target.querySelector("[data-mock-exit]")?.addEventListener("click",renderDiscussionHome)
}
function checklistMarkup(flags){const labels={what:"I gave the correct action or main point.",how:"I explained how I would apply it.",why:"I explained why it matters.",check:"I described a check or confirmation.",example:"I linked it to work or portfolio evidence."};return '<div class="evia-arp-checklist">'+Object.entries(labels).map(([key,label])=>'<label class="evia-arp-check"><input type="checkbox" data-mock-check="'+key+'" '+(flags?.[key]?"checked":"")+'><span>'+esc(label)+'</span></label>').join("")+'</div>'}
function renderMockReview(question,recording){
  const target=body();if(!target||!session)return;const strong=question.responses[0],flags=recording?.transcript?coverageFlags(question,recording.transcript):null;
  target.innerHTML='<p class="evia-tools-kicker">Mock self-check</p><h2>'+esc(question.topic)+'</h2>'+audioMarkup(recording)+transcriptMarkup(recording)+'<div class="evia-arp-guide"><b>Thorough answer guide</b>'+esc(strong.text)+'</div><div class="evia-arp-followup"><b>Evia follow-up</b>'+esc(question.followUp)+'</div><p class="evia-tools-copy">'+(flags?"Evia has suggested the points heard in the device transcript. Adjust the checks after listening back.":"Listen back or reflect on what you said, then check the points you genuinely covered.")+'</p>'+checklistMarkup(flags)+'<button type="button" class="evia-tools-primary" data-mock-score>Score and continue</button>';
  target.querySelector("[data-mock-score]")?.addEventListener("click",()=>{const covered=target.querySelectorAll("[data-mock-check]:checked").length,strength=covered<=1?1:covered===2?2:covered<=4?3:4;session.results.push({id:question.id,topic:question.topic,strength,question});session.index+=1;releaseVoice();if(session.index>=MOCK_SIZE)renderDiscussionResult();else renderMockQuestion()})
}
function onDiscussionClick(event){if(event.target?.closest?.('.evia-arp-layer [data-arp-back]'))releaseVoice();const button=event.target?.closest?.('.evia-arp-layer [data-arp-option="discussion"]');if(!button)return;event.preventDefault();event.stopPropagation();event.stopImmediatePropagation?.();renderDiscussionHome()}
function ready(){patchDiscussionRow();document.addEventListener("click",onDiscussionClick,true)}
if(document.readyState==="loading")document.addEventListener("DOMContentLoaded",ready);else ready();
window.addEventListener("pageshow",patchDiscussionRow);window.addEventListener("pagehide",releaseVoice);
window.EviaArpDiscussion={open:renderDiscussionHome,start:startDiscussion,discussionItems,responsesFor,coverageFlags};
})();
