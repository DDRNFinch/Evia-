{
  "version": "203",
  "title": "One light completion tick only",
  "details": [
    "Dark filled evidence-source tick circles have been removed from Evia",
    "The original light completion circle with its tick is kept as the single completion marker",
    "Old dark marker classes are actively removed if a cached screen tries to restore them",
    "The v202 faster photo review remains unchanged"
  ],
  "summary": "Evia v203 permanently removes the dark duplicate tick circles and keeps the light completion tick.",
  "changes": [
    "Removed dark learner, RPL, assessor and witness marker circles from the active UI",
    "Course coverage now uses one light tick for any evidenced KSB",
    "Evidence opportunity rows use one light completion tick instead of stacked source markers",
    "Added a legacy-marker cleanup guard so old dark circles cannot reappear"
  ]
}
